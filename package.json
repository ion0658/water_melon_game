{
    "name": "water_melon_game",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "wasm-pack build ./wasm-lib --target web && vite",
        "build": "wasm-pack build ./wasm-lib --target web && tsc && vite build",
        "preview": "vite preview",
        "test": "vitest",
        "coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^0.34.6",
        "typescript": "^5.0.2",
        "vite": "^4.4.11",
        "vite-plugin-wasm-pack": "^0.1.12",
        "vitest": "^0.34.6",
        "wasm-pack": "^0.12.1"
    }
}
