{
    "name": "water_melon_game",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "wasm": "wasm-pack build ./wasm-lib --target web",
        "dev": "npm run wasm && vite",
        "build": "npm run wasm && tsc && vite build",
        "preview": "vite preview",
        "test": "npm run wasm  && cd ./wasm-lib && cargo test"
    },
    "dependencies": {
        "wasm-lib": "file:./wasm-lib/pkg"
    },
    "devDependencies": {
        "typescript": "^5.0.2",
        "vite": "^4.4.11",
        "wasm-pack": "^0.12.1"
    }
}
